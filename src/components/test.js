/**
 * It takes two numbers as arguments and returns the sum of those two numbers
 * @param a - The first number to add.
 * @param b - The second number to add.
 * @returns The sum of a and b.
 */
export function addTwoNumbers(a, b) {
  return a + b;
}
